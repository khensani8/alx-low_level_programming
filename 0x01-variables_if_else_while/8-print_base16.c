#include <stdio.h>
/**
  *main - Entry Point
  *Description: 'print all numbers of base 16 in lowercase'
  *Return: Always 0
  */
int main(void)
{
	int n;
	int m;

	for (n = 48; n <= 57; n++)
	{
		putchar(n);
	}
	for (m = 97; m <= 102; n++)
	{
		putchar(m);
	}
	return (0);
}
